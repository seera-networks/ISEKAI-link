[package]
name = "portal-server"
version.workspace = true
edition = "2021"

# The side with the services (plan §4.2). Phase 1c-iii-c-ii: it connects for
# real, and the catalogue is still built from arguments rather than the file
# phase 2 adds.
[dependencies]
portal-core = { workspace = true }
isekai-p2p = { workspace = true }
anyhow = { workspace = true }
argh = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
