[package]
name = "portal-client"
version = "0.1.0"
edition = "2021"

# The side with the local ports (plan §4.2). Phase 1c-iii-c-ii.
[dependencies]
portal-core = { workspace = true }
isekai-p2p = { workspace = true }
# Watching the peer connection for its own end (`closed`).
msquic-async = { workspace = true }
anyhow = { workspace = true }
argh = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
