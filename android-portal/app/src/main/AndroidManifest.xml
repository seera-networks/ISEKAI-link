<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".PortalApplication"
        android:label="Portal Client"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Auth0 Authorization Code + PKCE redirect. Same scheme as the
                 camera apps (Kozuka's unified callback decision), see
                 Auth0Config.kt's CALLBACK_SCHEME comment for the tradeoff
                 that implies when more than one ISEKAI app is installed. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="isekaiviewer" android:host="callback" />
            </intent-filter>
        </activity>
    </application>
</manifest>
