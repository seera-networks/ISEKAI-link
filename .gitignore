/rust/target
/target

# Endpoint private keys generated by the camera apps / isekai-p2p CLIs and the
# relay_e2e example (load_or_generate_key). Long-lived secrets — never commit.
*-endpoint.pem
e2e-server.pem
e2e-client.pem
